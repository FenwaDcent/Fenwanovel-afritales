:root{
  --bg:#0d0d0f;
  --panel:#141418;
  --soft:#1a1b21;
  --text:#f2f2f2;
  --muted:#b9bac4;
  --brand:#f8c94a;
  --brand-2:#ffd95a;
  --link:#9dd4ff;
  --ok:#4ade80;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{
  font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
  background:linear-gradient(180deg,#0b0c0e, var(--bg));
  color:var(--text);
}
a{color:inherit;text-decoration:none}
header{
  position:sticky;top:0;z-index:20;
  background:linear-gradient(90deg,#111,#191922);
  border-bottom:1px solid #22232a;
  padding:.9rem 1rem;
  display:flex;align-items:center;gap:.8rem;
}
.logo{
  display:inline-flex;align-items:center;gap:.6rem;font-weight:800;letter-spacing:.3px
}
.logo-badge{width:10px;height:10px;border-radius:50%;background:var(--brand)}
.brand{color:var(--text);font-size:1.05rem}
.sub{color:var(--muted);font-weight:600}
main{max-width:1024px;margin:1rem auto 3rem;padding:0 1rem}
.topbar{
  display:flex;align-items:center;gap:1rem;justify-content:space-between;
  margin:.75rem 0 1rem;
}
.chips{display:flex;flex-wrap:wrap;gap:.5rem}
.chip{
  background:var(--soft);border:1px solid #262732;color:var(--muted);
  padding:.38rem .7rem;border-radius:999px;font-size:.85rem
}
.balance{font-size:.85rem;color:var(--muted)}
.grid{
  display:grid;gap:1rem;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
}
.card{
  background:var(--panel);border:1px solid #22232a;
  border-radius:14px;overflow:hidden;box-shadow:0 10px 30px rgba(0,0,0,.25);
}
.cover{width:100%;height:210px;object-fit:cover;background:#0b0b0d}
.card-body{padding:1rem}
.card h3{margin:.2rem 0 .3rem;font-size:1.05rem;color:var(--text)}
.card p{margin:0;color:var(--muted);font-size:.92rem;min-height:2.8em}
.card .tags{margin-top:.6rem;display:flex;gap:.4rem;flex-wrap:wrap}
.tag{font-size:.72rem;background:#20222b;border:1px solid #2a2c38;color:#cbd5e1;padding:.22rem .5rem;border-radius:40px}
.actions{display:flex;gap:.5rem;margin-top:.8rem}
.btn{
  display:inline-flex;align-items:center;justify-content:center;gap:.5rem;
  padding:.55rem .9rem;border-radius:10px;border:1px solid #2a2c38;cursor:pointer;
  background:#1a1b21;color:var(--text);font-weight:700;font-size:.9rem
}
.btn.primary{background:var(--brand);border-color:var(--brand);color:#111}
.btn.primary:hover{background:var(--brand-2)}
.btn.ghost:hover{border-color:#383a46;background:#1f2030}
footer{margin:2.2rem 0;color:#8b8d98;text-align:center;font-size:.85rem}
hr{border:0;border-top:1px solid #22232a;margin:1.5rem 0}
.book-hero{
  display:grid;grid-template-columns:140px 1fr;gap:1rem;align-items:center;
  background:var(--panel);border:1px solid #22232a;border-radius:14px;padding:1rem;
}
.book-hero img{width:140px;height:190px;object-fit:cover;border-radius:10px}
.meta h1{font-size:1.35rem;margin:.1rem 0 .2rem}
.meta .by{color:var(--muted);font-size:.92rem}
.meta .blurb{color:#cdd1de;margin:.4rem 0 0}
.chapter{
  background:#121218;border:1px solid #22232a;border-left:3px solid var(--brand);
  border-radius:10px;padding:.8rem .9rem;margin:.6rem 0
}
.chapter h4{margin:.1rem 0 .4rem;font-size:1rem;color:#f7f7f8}
.chapter p{margin:0;color:#c5c8d3}
.back{display:inline-block;margin-top:.8rem;color:var(--link)}
